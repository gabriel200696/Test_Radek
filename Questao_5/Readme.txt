Visual Studio Code 2022 software was used to create the requested script.
 
To execute the Unit Test, for the function ConcatRemove(s,t,k), it is necessary to select the test file ConcatRemove_Unit_Test.cpp and first Build Solution.

Later, through the Test Explorer tool, the tests can be executed and evaluated according to the respective assertions.

As tests, the three described in the previous question were carried out, in addition to other tests. To insert a new test, just add a TEST_METHOD and manually inform the input variables s, t and K to the ConcatRemove(s,t,k) function, as well as its respective Assert.